Thanks, Sathwik. Based on the **project proposal** and **tech stack prototype**, your role is to implement **email integration**, and although the proposal initially mentions Gmail, you're now dealing with **GoDaddy email** (likely IMAP/SMTP-based).

Since you're the first contributor, I'll break your tasks into 4 clear **phases**—from setup to integration—assuming the system will still abstract the email provider using an `EmailClient` interface.

---

### ✅ Phase 1: Environment & Abstract Email Client Setup

**Goal:** Lay the foundation for a modular email system.

**Tasks:**

* [ ] Set up local dev environment with Docker Compose.
* [ ] Create the `src/email/` module with:

  * `client.py` → Define an abstract base class `EmailClient`.
  * `godaddy_client.py` → Start building GoDaddy-specific implementation using IMAP/SMTP.
* [ ] Set up `.env` file for storing:

  * IMAP/SMTP server credentials.
  * Email addresses and ports.
* [ ] Write helper functions for:

  * IMAP login
  * Listing inbox
  * Fetching unread emails
  * Extracting attachments (PDFs)
  * Sending templated responses via SMTP

---

### ✅ Phase 2: Implement GoDaddy Email Ingestion & Sending

**Goal:** Handle basic read/send operations and test interaction with EOs.

**Tasks:**

* [ ] Build and test `fetch_unread_emails()` with PDF filtering.
* [ ] Save downloaded PDFs to a temp folder for processing.
* [ ] Parse email metadata (From, Subject, Date) and store it in a temp data structure.
* [ ] Implement `send_email(to, subject, body, attachments=None)` via SMTP.
* [ ] Write unit tests or simple test scripts for basic send/receive roundtrip.

> ✅ Optional: Implement a minimal caching layer to avoid reprocessing the same emails.

---

### ✅ Phase 3: Integrate With Celery Workflow

**Goal:** Make your email logic asynchronous and production-ready.

**Tasks:**

* [ ] Move email polling into a Celery task (`tasks/email_ingest.py`).
* [ ] Schedule polling (e.g., every 10 mins) using Celery Beat.
* [ ] After PDF is fetched, enqueue it for the LLM directive extraction (`ai/extract_directives.py`).
* [ ] Log all email actions to PostgreSQL (emails received, responses sent, etc.).

---

### ✅ Phase 4: Response Handling & Abstraction Completion

**Goal:** Enable intelligent email workflows and finalize modularity.

**Tasks:**

* [ ] Implement parsing of incoming responses:

  * Detect "Accepted", "Declined", status updates using regex + GPT if needed.
* [ ] Finalize the `GodaddyEmailClient` to conform to `EmailClient` API.
* [ ] Add fallback logic if GoDaddy has IMAP/SMTP quirks (e.g., slow sync, sent folder issues).
* [ ] Document how to plug in any email provider (Gmail, Outlook) just by swapping the client class.
* [ ] Work with the AI/NLP dev to format inputs/outputs from GPT to match email UX needs.

---

### Final Deliverables for Your Role

1. `email/client.py` – Abstract base
2. `email/godaddy_client.py` – Full IMAP/SMTP integration
3. `tasks/email_ingest.py` – Celery tasks for polling/sending
4. `.env.template` – Sample credentials config
5. Test scripts for read/send cycle
6. Documentation on how to extend or swap email providers

---

Let me know if you want help scaffolding `client.py` or setting up IMAP for GoDaddy.
